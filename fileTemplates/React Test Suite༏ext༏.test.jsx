import React from 'react';
import {shallow} from 'enzyme';

import ${NAME} from '../../src/$path/${NAME}';

describe('${NAME}', () => {
    it('should', () => {
        const wrapper = shallow(<${NAME}/>);
    });
});
