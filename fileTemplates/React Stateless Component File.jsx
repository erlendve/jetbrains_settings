import React from 'react';
import PropTypes from 'prop-types';

function ${NAME}(props) {
    return (
        <div></div>
    );
}

${NAME}.propTypes = {};
${NAME}.defaultProps = {};

export default ${NAME};